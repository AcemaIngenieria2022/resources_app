// Servicio placeholder para la gestión de roles del sistema.
export const roleService = {
  list: async () => [],
  create: async () => ({ success: true }),
  update: async () => ({ success: true }),
};

export default roleService;
