// Servicio placeholder para generación y exportación de reportes.
export const reportService = {
  generate: async () => ({ success: true }),
  exportCsv: async () => ({ success: true }),
};

export default reportService;
